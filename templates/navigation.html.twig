<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Symericipe</title>
<link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
{% block stylesheets %}

<!-- Bootstrap core CSS -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-TszuL7VgQ9XWb6VZUcIfiK6bs71w6zzWUbiIKpnx+alqu0oDkG9XKjOZkwEpCZXL" crossorigin="anonymous">

<!-- Bootswatch theme -->
<link href="{{ asset('bootswatch/flatly.css') }}" rel="stylesheet">


    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.min.js"></script>
{% endblock %}
{% block javascripts %}{{ encore_entry_script_tags('app') }}{% endblock %}
</head>

<body>

{% block nav %}

{% if app.user %}

<nav class="navbar navbar-expand-lg navbar-dark bg-secondary">
  <div class="container-fluid">

    <div class="collapse navbar-collapse" id="navbarColor02">
      <ul class="navbar-nav me-auto">

        <li class="nav-item">
          <a class="nav-link active-light" href="{{ path('app_home') }}">Accueil</a>
        </li>

        <li class="nav-item">
          <a class="nav-link active-light" href="{{ path('app_ingredient') }}">Mes ingrédients</a>
        </li>

        <li class="nav-item">
          <a class="nav-link active-light" href="{{ path('app_recipe') }}">Mes recettes</a>
        </li>

        <li class="nav-item">
          <a class="nav-link active-light" href="{{ path('app.publique') }}">Recette partagée par la communauté</a>
        </li>

      </ul>    

        <div class="d-flex">
          <ul class="navbar-nav me-auto">

        <li class="nav-item"><a class="nav-link active-light" href="{{ path('app_modification', { id: app.user.id}) }}">Modifier le profil</a></li>

            <li class="nav-item"><a class="nav-link active-light" href="{{ path('app_deconnexion') }}">Déconnexion</a></li>

          </ul>
        </div>
    </div>
  </div>
</nav>

{% else %}
<nav class="navbar navbar-expand-lg navbar-dark bg-secondary"></nav>

{% endif %}

{% endblock %}

</body>
</html>
